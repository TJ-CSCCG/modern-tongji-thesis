# 字体文件

本模板**不再**通过 `fonts` 分支分发字体文件。

- 默认字体集 `fandol` 随 TeX Live 安装，Linux 用户可通过 `apt install fonts-fandol` 安装
- 如需其他字体集（`founder`、`adobe`），请访问 [cjk-fonts-for-ctex](https://github.com/TJ-CSCCG/cjk-fonts-for-ctex)
- Web App 用户可使用 `noto` 字体集（零配置）
- 详见 [README.md](../README.md#字体选择)
